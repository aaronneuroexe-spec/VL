# План внедрения компонентов в VoxLink

## 📦 Анализ компонентов в папке "Внедрять"

### 1. **coturn-master** - TURN/STUN сервер
**Текущее состояние:**
- ✅ Уже есть конфигурация в `infra/coturn/turnserver.conf`
- ✅ Используется Docker образ в `infra/docker-compose.yml`
- ⚠️ Исходники для кастомной сборки

**Рекомендация:**
- **Оставить как есть** - используем готовый Docker образ `coturn/coturn`
- Исходники можно использовать для:
  - Кастомной сборки с дополнительными опциями
  - Изучения и отладки
  - Создания собственного образа с оптимизациями

**Приоритет:** 🟢 Низкий (уже работает)

---

### 2. **opus-main** - Аудио кодек Opus
**Текущее состояние:**
- ❌ Не интегрирован
- ✅ Нужен для высококачественного аудио в WebRTC

**Что нужно:**
- Собрать библиотеку `libopus` для backend
- Интегрировать в Node.js через native addon или FFI
- Использовать для:
  - Кодирования/декодирования аудио
  - Адаптивного битрейта
  - Улучшения качества при потере пакетов

**План внедрения:**
1. Собрать libopus для целевой платформы
2. Создать Node.js wrapper (node-opus или использовать FFI)
3. Интегрировать в `backend/src/media/media.service.ts`
4. Добавить поддержку Opus в WebRTC настройки

**Приоритет:** 🟡 Средний (улучшит качество аудио)

---

### 3. **webrtc-main** - WebRTC библиотека
**Текущее состояние:**
- ❌ Не интегрирован
- ✅ WebRTC используется только на frontend (браузерный API)

**Что нужно:**
- Нативный WebRTC на backend для:
  - SFU (Selective Forwarding Unit) - масштабирование
  - Запись звонков
  - Транскодирование
  - Микширование аудио

**План внедрения:**
1. Использовать готовые бинарники из `webrtc-main`
2. Создать Node.js addon для WebRTC API
3. Интегрировать в `backend/src/media/media.service.ts`
4. Реализовать SFU для масштабирования

**Приоритет:** 🟠 Высокий (для масштабирования и записи)

---

### 4. **ws-master** - WebSocket библиотека
**Текущее состояние:**
- ✅ Используется Socket.IO (`socket.io`)
- ⚠️ `ws` - более легковесная альтернатива

**Что нужно:**
- Оценить необходимость замены Socket.IO на `ws`
- Преимущества `ws`:
  - Меньший размер
  - Лучшая производительность
  - Проще для простых случаев
- Недостатки:
  - Нет автоматического переподключения
  - Нет комнат из коробки
  - Меньше функций

**План внедрения:**
1. Проанализировать использование Socket.IO
2. Если нужна только базовая функциональность - заменить на `ws`
3. Если нужны комнаты/переподключение - оставить Socket.IO или добавить обёртку

**Приоритет:** 🟢 Низкий (оптимизация, не критично)

---

## 🎯 Рекомендуемый порядок внедрения

### Фаза 1: Критичные компоненты (Сейчас)
1. ✅ **coturn** - уже работает через Docker
2. ⏳ **webrtc-main** - для SFU и записи (высокий приоритет)

### Фаза 2: Улучшения качества (Следующий этап)
3. ⏳ **opus-main** - для улучшения качества аудио

### Фаза 3: Оптимизация (По желанию)
4. ⏳ **ws-master** - замена Socket.IO (если нужно)

---

## 📋 Детальный план внедрения WebRTC

### Шаг 1: Подготовка WebRTC библиотеки
```bash
# Использовать готовые бинарники из webrtc-main
# Или собрать через скрипты в webrtc-main/scripts/
```

### Шаг 2: Создание Node.js addon
- Использовать `node-addon-api` или `node-ffi`
- Обернуть WebRTC C++ API в JavaScript

### Шаг 3: Интеграция в Media Service
- Добавить SFU функциональность
- Реализовать запись звонков
- Добавить транскодирование

### Шаг 4: Тестирование
- Тесты производительности
- Тесты масштабирования
- Интеграционные тесты

---

## 📋 Детальный план внедрения Opus

### Шаг 1: Сборка libopus
```bash
cd Внедрять/opus-main
./autogen.sh
./configure
make
make install
```

### Шаг 2: Node.js интеграция
- Использовать `node-opus` или `@discordjs/opus`
- Или создать свой wrapper через FFI

### Шаг 3: Интеграция в WebRTC
- Настроить Opus как основной кодек
- Добавить адаптивный битрейт
- Улучшить обработку потери пакетов

---

## ⚠️ Важные замечания

1. **Лицензии:**
   - Opus - BSD/ISC (свободная)
   - WebRTC - BSD (свободная)
   - coturn - BSD (свободная)
   - ws - MIT (свободная)

2. **Зависимости:**
   - WebRTC требует много зависимостей (depot_tools, ninja, etc.)
   - Opus требует autotools для сборки
   - Все компоненты требуют C/C++ компилятор

3. **Платформы:**
   - WebRTC бинарники доступны для Windows, Mac, Linux
   - Opus нужно собирать для каждой платформы
   - coturn работает через Docker (кроссплатформенно)

4. **Производительность:**
   - WebRTC addon может быть сложным в отладке
   - Opus требует оптимизации под платформу
   - Рекомендуется профилирование

---

## 🚀 Быстрый старт (рекомендуемый порядок)

1. **Начать с WebRTC** - самый критичный компонент
2. **Добавить Opus** - улучшит качество
3. **Оптимизировать WebSocket** - если нужно

---

## 📚 Полезные ссылки

- [WebRTC Native API](https://webrtc.googlesource.com/src/)
- [Opus Codec](https://opus-codec.org/)
- [coturn Documentation](https://github.com/coturn/coturn)
- [ws Documentation](https://github.com/websockets/ws)

---

**Готов начать внедрение?** 🚀