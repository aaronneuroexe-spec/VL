# VoxLink Windows Scripts

Руководство по использованию скриптов VoxLink на Windows.

## 🪟 Windows-совместимые скрипты

### Быстрый запуск

#### Вариант 1: PowerShell скрипт (рекомендуется)

```powershell
# Просто запустите PowerShell скрипт
.\scripts\quick-test.ps1
```

#### Вариант 2: Batch файл

```cmd
# Двойной клик или запуск из командной строки
.\scripts\quick-test.bat
```

#### Вариант 3: EXE файл (самый удобный)

```powershell
# Создать EXE файл
.\scripts\build-exe.ps1

# Запустить созданный EXE
.\VoxLink-QuickTest.exe
```

## 📦 Создание EXE файла

### Метод 1: Использование PS2EXE (автоматический)

```powershell
# 1. Установить PS2EXE модуль (требуются права администратора)
Install-Module -Name ps2exe -Force -Scope CurrentUser

# 2. Создать EXE
.\scripts\build-exe.ps1

# 3. Запустить
.\VoxLink-QuickTest.exe
```

### Метод 2: Использование WinRAR/Self-Extractor

1. Упакуйте `quick-test.ps1` в самораспаковывающийся архив
2. Настройте на запуск: `powershell.exe -ExecutionPolicy Bypass -File quick-test.ps1`

### Метод 3: Использование IExpress (встроенный в Windows)

```cmd
# Создать SED файл для IExpress
# Затем использовать IExpress для создания EXE
```

### Метод 4: Использование AutoIt или подобных инструментов

Создайте простой AutoIt скрипт, который запускает PowerShell скрипт.

## 🔧 Требования

### Для PowerShell скрипта:
- ✅ Windows 10/11 или Windows Server 2016+
- ✅ PowerShell 5.1+ (встроен в Windows)
- ✅ Docker Desktop для Windows
- ✅ docker-compose

### Для EXE файла:
- ✅ Все требования выше
- ✅ PS2EXE модуль (для создания EXE)

## 🚀 Использование

### Запуск тестов

1. **Убедитесь что Docker запущен**
   ```powershell
   docker ps
   ```

2. **Убедитесь что сервисы VoxLink запущены**
   ```powershell
   cd infra
   docker-compose up -d
   cd ..
   ```

3. **Запустите тест**
   ```powershell
   .\scripts\quick-test.ps1
   ```

### Что проверяет скрипт

- ✅ Docker установлен и запущен
- ✅ docker-compose доступен
- ✅ Сервисы VoxLink запущены
- ✅ Health endpoint работает
- ✅ Frontend доступен
- ✅ API документация доступна
- ✅ База данных подключена
- ✅ Redis подключен
- ✅ WebSocket порт открыт
- ✅ API endpoints работают
- ✅ Metrics endpoint работает
- ✅ Проверка логов на ошибки
- ✅ Проверка дискового пространства
- ✅ Проверка использования памяти

## 🛠️ Устранение проблем

### Проблема: "Execution Policy"

Если получаете ошибку о политике выполнения:

```powershell
# Разрешить выполнение скриптов для текущей сессии
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process

# Или для текущего пользователя
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: Docker не найден

Убедитесь что Docker Desktop запущен и добавлен в PATH:

```powershell
# Проверить
docker --version

# Если не работает, добавьте Docker в PATH вручную
```

### Проблема: Сервисы не запущены

```powershell
# Перейти в директорию infra
cd infra

# Запустить сервисы
docker-compose up -d

# Проверить статус
docker-compose ps
```

### Проблема: Порт занят

Если порты 3000 или 4000 заняты:

```powershell
# Проверить что использует порт
netstat -ano | findstr :4000
netstat -ano | findstr :3000

# Остановить конфликтующий процесс или изменить порты в docker-compose.yml
```

## 📝 Альтернативные методы

### Использование WSL (Windows Subsystem for Linux)

Если у вас установлен WSL, вы можете использовать оригинальные bash скрипты:

```bash
# В WSL
cd /mnt/d/VoxLink
./scripts/quick-test.sh
```

### Использование Git Bash

```bash
# В Git Bash
cd /d/VoxLink
./scripts/quick-test.sh
```

## 🎯 Рекомендации

1. **Используйте PowerShell ISE или VS Code** для редактирования скриптов
2. **Создайте ярлык на рабочем столе** для быстрого доступа к EXE
3. **Добавьте в PATH** для запуска из любого места
4. **Используйте планировщик задач Windows** для автоматического запуска

## 📚 Дополнительная информация

- [PowerShell Documentation](https://docs.microsoft.com/powershell/)
- [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop)
- [PS2EXE Module](https://www.powershellgallery.com/packages/ps2exe)

---

**Готово к использованию на Windows!** 🪟✅