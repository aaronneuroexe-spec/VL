# VoxLink для Windows - Быстрый старт

## 🪟 Запуск тестов на Windows

### Самый простой способ (рекомендуется)

1. **Двойной клик** на файл:
   ```
   scripts\quick-test-launcher.bat
   ```

Этот файл автоматически:
- ✅ Проверит что Docker запущен
- ✅ Запустит сервисы если они не запущены
- ✅ Выполнит все тесты
- ✅ Покажет результаты

### Альтернативные способы

#### Вариант 1: PowerShell скрипт

```powershell
# Откройте PowerShell в папке проекта
.\scripts\quick-test.ps1
```

#### Вариант 2: Batch файл

```cmd
# Двойной клик или из командной строки
.\scripts\quick-test.bat
```

#### Вариант 3: Создать EXE файл

```powershell
# 1. Установить PS2EXE (один раз, требует админ права)
Install-Module -Name ps2exe -Force -Scope CurrentUser

# 2. Создать EXE
.\scripts\build-exe.ps1

# 3. Запустить созданный EXE
.\VoxLink-QuickTest.exe
```

## 📋 Что нужно перед запуском

1. **Docker Desktop** должен быть установлен и запущен
   - Скачать: https://www.docker.com/products/docker-desktop

2. **VoxLink сервисы** должны быть запущены
   ```cmd
   cd infra
   docker-compose up -d
   ```

3. **PowerShell** (встроен в Windows 10/11)

## 🚀 Быстрый тест

### Шаг 1: Запустите Docker Desktop

Убедитесь что Docker Desktop запущен (иконка в трее должна быть зелёной).

### Шаг 2: Запустите тест

**Самый простой способ:**
- Двойной клик на `scripts\quick-test-launcher.bat`

**Или из командной строки:**
```cmd
cd D:\VoxLink
scripts\quick-test-launcher.bat
```

### Шаг 3: Смотрите результаты

Скрипт покажет:
- ✅ Какие тесты прошли успешно
- ❌ Какие тесты провалились
- 📊 Итоговую статистику

## 🔧 Если что-то не работает

### Ошибка: "Execution Policy"

```powershell
# Запустите PowerShell от имени администратора и выполните:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Ошибка: "Docker is not running"

1. Запустите Docker Desktop
2. Дождитесь полной загрузки (иконка в трее зелёная)
3. Попробуйте снова

### Ошибка: "Services are not running"

Скрипт `quick-test-launcher.bat` автоматически запустит сервисы.
Или вручную:
```cmd
cd infra
docker-compose up -d
```

### Ошибка: "Port already in use"

Порты 3000 или 4000 заняты другим приложением:
```cmd
# Проверить что использует порт
netstat -ano | findstr :4000
netstat -ano | findstr :3000
```

## 📁 Структура файлов

```
scripts/
├── quick-test.ps1              ← PowerShell скрипт (основной)
├── quick-test.bat              ← Batch обёртка
├── quick-test-launcher.bat      ← Умный launcher (рекомендуется)
├── build-exe.ps1                ← Создание EXE файла
└── README-WINDOWS.md            ← Подробная документация
```

## 🎯 Рекомендации

1. **Используйте `quick-test-launcher.bat`** - он самый умный
2. **Создайте ярлык на рабочем столе** для быстрого доступа
3. **Для автоматизации** используйте планировщик задач Windows

## 📚 Подробная документация

См. `scripts\README-WINDOWS.md` для детальной информации.

---

**Готово! Просто запустите `quick-test-launcher.bat`** 🚀