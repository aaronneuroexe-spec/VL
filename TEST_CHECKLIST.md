# VoxLink Test Checklist

Полный чеклист для тестирования VoxLink перед запуском в production.

## 🚀 Быстрый тест

```bash
# Запустить автоматический тест
./scripts/quick-test.sh

# Или ручной тест развёртывания
./scripts/test-deployment.sh
```

## ✅ Чеклист тестирования

### 1. Развёртывание и инфраструктура

- [ ] Все сервисы запускаются без ошибок
- [ ] Health check возвращает `healthy`
- [ ] Frontend доступен на порту 3000
- [ ] Backend доступен на порту 4000
- [ ] База данных подключена и работает
- [ ] Redis подключен и работает
- [ ] Nginx проксирует запросы корректно
- [ ] Docker контейнеры стабильны (не перезапускаются)

**Команды для проверки:**
```bash
docker-compose ps
curl http://localhost:4000/health
curl http://localhost:3000
docker-compose logs | grep -i error
```

### 2. Аутентификация

- [ ] Создание invite token работает
- [ ] Вход по invite token работает
- [ ] Magic link отправляется (если настроен SMTP)
- [ ] JWT токены генерируются корректно
- [ ] Токены истекают через заданное время
- [ ] Обновление токена работает
- [ ] Выход из системы работает

**Тест:**
```bash
# Создать invite
INVITE=$(curl -X POST http://localhost:4000/api/auth/invite \
  -H "Content-Type: application/json" \
  -d '{"role": "member"}' | jq -r '.inviteToken')

# Войти
curl -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"token\": \"$INVITE\", \"username\": \"testuser\"}"
```

### 3. Каналы

- [ ] Создание текстового канала
- [ ] Создание голосового канала
- [ ] Создание приватного канала
- [ ] Получение списка каналов
- [ ] Редактирование канала
- [ ] Удаление канала
- [ ] Поиск каналов
- [ ] Права доступа к приватным каналам

**Тест:**
```bash
TOKEN="your-jwt-token"
curl -X GET http://localhost:4000/api/channels \
  -H "Authorization: Bearer $TOKEN"
```

### 4. Сообщения

- [ ] Отправка текстового сообщения
- [ ] Получение истории сообщений
- [ ] Редактирование сообщения
- [ ] Удаление сообщения
- [ ] Ответ на сообщение (reply)
- [ ] Прикрепление файлов (если реализовано)
- [ ] Поиск сообщений
- [ ] Пагинация сообщений

**Тест:**
```bash
CHANNEL_ID="channel-uuid"
curl -X POST http://localhost:4000/api/channels/$CHANNEL_ID/messages \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"content": "Test message"}'
```

### 5. WebSocket

- [ ] Подключение к WebSocket
- [ ] Аутентификация через WebSocket
- [ ] Присоединение к каналу
- [ ] Получение сообщений в реальном времени
- [ ] Отправка сообщений через WebSocket
- [ ] Typing indicators
- [ ] Presence updates
- [ ] Переподключение при обрыве

**Тест:**
```javascript
// В браузерной консоли
const socket = io('http://localhost:4000', {
  auth: { token: 'your-jwt-token' }
});

socket.on('connect', () => console.log('Connected'));
socket.emit('join_channel', { channelId: 'uuid' });
```

### 6. Голосовые каналы (WebRTC)

- [ ] Получение WebRTC конфигурации
- [ ] Подключение к голосовому каналу
- [ ] Mute/Unmute микрофона
- [ ] Deafen/Undeafen
- [ ] Качество звука приемлемое
- [ ] Задержка < 500ms
- [ ] P2P соединение устанавливается
- [ ] TURN сервер используется при необходимости
- [ ] Отключение от канала

**Тест:**
1. Откройте два браузера
2. Войдите в систему в обоих
3. Присоединитесь к голосовому каналу
4. Проверьте что слышите друг друга

### 7. События

- [ ] Создание события
- [ ] Редактирование события
- [ ] Удаление события
- [ ] Получение списка событий
- [ ] Получение предстоящих событий
- [ ] Фильтрация по каналу

**Тест:**
```bash
curl -X POST http://localhost:4000/api/events \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Test Event",
    "startsAt": "2024-12-31T20:00:00Z",
    "channelId": "channel-uuid"
  }'
```

### 8. Администрирование

- [ ] Просмотр статистики системы
- [ ] Бан пользователя
- [ ] Разбан пользователя
- [ ] Удаление канала (admin)
- [ ] Удаление сообщения (admin)
- [ ] Просмотр активности пользователя

**Тест:**
```bash
# Требуется admin токен
curl -X GET http://localhost:4000/api/admin/stats \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

### 9. Производительность

- [ ] Время загрузки frontend < 3 секунд
- [ ] Время ответа API < 500ms
- [ ] WebSocket переподключение < 5 секунд
- [ ] Нет утечек памяти
- [ ] CPU usage в норме
- [ ] Memory usage в норме

**Тест:**
```bash
# Мониторинг ресурсов
docker stats

# Проверка метрик
curl http://localhost:4000/metrics
```

### 10. Безопасность

- [ ] Rate limiting работает
- [ ] CORS настроен корректно
- [ ] JWT токены валидируются
- [ ] Input validation работает
- [ ] SQL injection защита
- [ ] XSS защита
- [ ] HTTPS работает (в production)

**Тест:**
```bash
# Тест rate limiting
for i in {1..150}; do
  curl http://localhost:4000/api/channels
done
```

### 11. Мобильная версия

- [ ] Адаптивный UI работает
- [ ] Голосовые каналы работают на мобильном
- [ ] Push-to-talk работает
- [ ] PWA установка работает
- [ ] Уведомления работают

**Тест:**
1. Откройте на мобильном устройстве
2. Проверьте все функции
3. Установите как PWA

### 12. Мониторинг

- [ ] Health endpoint работает
- [ ] Metrics endpoint работает
- [ ] Логи записываются
- [ ] Ошибки логируются
- [ ] Prometheus метрики доступны (если настроено)

**Тест:**
```bash
curl http://localhost:4000/health
curl http://localhost:4000/metrics
docker-compose logs --tail=50
```

### 13. Резервное копирование

- [ ] Backup скрипт работает
- [ ] Backup создаётся корректно
- [ ] Восстановление из backup работает
- [ ] Автоматические backup работают

**Тест:**
```bash
./scripts/backup.sh backup
./scripts/backup.sh list
./scripts/backup.sh restore /backups/voxlink/backup.sql.gz
```

### 14. Документация

- [ ] README актуален
- [ ] API документация актуальна
- [ ] Deployment guide актуален
- [ ] FAQ актуален
- [ ] Все примеры кода работают

**Проверка:**
- Прочитайте всю документацию
- Попробуйте все примеры
- Проверьте ссылки

## 🎯 Критерии готовности к production

### Минимальные требования

- ✅ Все сервисы запускаются
- ✅ Аутентификация работает
- ✅ Каналы и сообщения работают
- ✅ WebSocket работает
- ✅ Голосовые каналы работают (хотя бы для 2-3 пользователей)
- ✅ Health check работает
- ✅ Backup работает

### Рекомендуемые требования

- ✅ Все тесты из чеклиста пройдены
- ✅ Нагрузочное тестирование пройдено
- ✅ Мониторинг настроен
- ✅ SSL сертификаты установлены
- ✅ Документация актуальна
- ✅ Резервное копирование автоматизировано

## 📊 Отчёт о тестировании

После завершения тестирования создайте отчёт:

```markdown
# Test Report - VoxLink

## Date: [Date]
## Tester: [Name]
## Environment: [Local/Staging/Production]

### Results
- Total Tests: [X]
- Passed: [X]
- Failed: [X]
- Skipped: [X]

### Issues Found
1. [Issue description]
2. [Issue description]

### Recommendations
- [Recommendation]
```

## 🚀 Следующие шаги

После успешного тестирования:

1. Разверните на staging окружении
2. Проведите бета-тест с реальными пользователями
3. Соберите обратную связь
4. Исправьте найденные проблемы
5. Разверните на production

---

**Готово к тестированию!** 🎉